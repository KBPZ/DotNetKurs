<UserControl x:Class="Vending.Client.Main.AutomatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Vending.Client.Main"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid d:DataContext="{d:DesignInstance {x:Type local:MainViewVM}}">

        <!--Товары автомата-->
        <DockPanel Grid.Row="0" Grid.Column="0">
            <Label DockPanel.Dock="Top" Content="Товары"/>

            
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom">
                <Button Command="{Binding AddTest}" Margin="5">Добавить</Button>
                <Button Command="{Binding BuyTest}" Margin="5">Купить</Button>
                <Button Command="{Binding ChangeTest}" Margin="5">Изменить</Button>
            </StackPanel> 
            <ListBox ItemsSource="{Binding ProductsInAutomata}"/>
        </DockPanel>
    </Grid>
</UserControl>
